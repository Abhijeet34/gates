# Changelog

This project's release line starts at `0.1.0`. Nothing above `0.1.0` was released
from this repository, and no entry for `0.1.0` exists yet because no release has
been cut on that line.

The single entry below is the fork point, not a release of this project. Its
links point at the upstream repository, because that is where those commits were
published; README's "Where this repository sits" names it. The commits are in
this tree's history as well, so the links resolve to code this package actually
ships. Removing that entry would leave `release-please` no version heading to
insert above, so it stays.

The 52 older inherited entries were removed from this file, not from the record.
`git log -p --follow -- CHANGELOG.md` still shows every one of them.

## [0.1.53](https://github.com/kunchenguid/lavish-axi/compare/lavish-axi-v0.1.52...lavish-axi-v0.1.53) (2026-08-18)


### Bug Fixes

* avoid phantom whiteboard conflicts ([#252](https://github.com/kunchenguid/lavish-axi/issues/252)) ([196d24f](https://github.com/kunchenguid/lavish-axi/commit/196d24f132e6361a432509e8f634ba16b8976154))
